<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.feedthecat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.feedthecat.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.feedthecat.LoginActivity"/>
        <activity android:name="com.example.feedthecat.RegisterActivity"/>
        <activity android:name="com.example.feedthecat.TapActivity"/>
        <activity android:name="com.example.feedthecat.MenuActivity"/>
        <activity android:name="com.example.feedthecat.ProfileActivity"/>
        <activity android:name="com.example.feedthecat.LeaderboardActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>